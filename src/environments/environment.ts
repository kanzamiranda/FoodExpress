// ============================================================
// FoodExpress — Environments Angular
// Produção (Render)
// ============================================================

export const environment = {
  production: true,
  // URL do backend PHP no Render
  // Após o deploy, substituir pelo URL real do serviço foodexpress-api
  apiUrl: 'https://foodexpress-api.onrender.com/api',
};
